{
  "name": "fgfwsub-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:edgeone": "node scripts/build-edgeone.mjs",
    "deploy": "edgeone makers deploy",
    "deploy:edgeone": "edgeone makers deploy",
    "dev:cloudflare": "wrangler dev",
    "deploy:cloudflare": "wrangler deploy",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.0.0",
    "@types/node": "^26.4.1",
    "edgeone": "^1.6.33",
    "esbuild": "^0.28.2",
    "typescript": "^5.5.2",
    "vitest": "~4.1.0",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "qrcode-generator": "2.0.4",
    "yaml": "^2.8.1"
  }
}
